# Copy this file to .env and fill in the values.
# Never commit the real .env to Git.

# Google Gemini API key — https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Primary Gemini model (used normally)
GEMINI_PRIMARY_MODEL=gemini-3.6-flash

# Fallback model — used only on quota/rate-limit errors (optional)
# Supports structured output and function calling
GEMINI_FALLBACK_MODEL=gemini-3.1-flash-lite

# Absolute or relative path to the source PDF
SOURCE_PDF=data/fy2024_analysis_of_revenue_and_expenditure.pdf
