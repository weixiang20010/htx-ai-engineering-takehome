# Copy this file to .env and fill in the values.
# Never commit the real .env to Git.

# Google Gemini API key — https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Extraction tasks (Part 1, Part 2 date extraction)
# 3.5 Flash-Lite: 15 RPM / 500 RPD, optimised for document parsing + structured output.
# Fallback to 3.1 Flash-Lite (same quota tier, same capabilities) on quota/provider errors.
GEMINI_EXTRACTION_MODEL=gemini-3.5-flash-lite
GEMINI_EXTRACTION_FALLBACK_MODEL=gemini-3.1-flash-lite

# Reasoning tasks (Part 2 MCP tool selection + temporal classification)
# 3.6 Flash: 5 RPM / 20 RPD — protected by routing extraction to 3.5 Lite.
# 3.1 Flash-Lite: common fallback for both tiers; supports structured output + function calling.
GEMINI_REASONING_MODEL=gemini-3.6-flash
GEMINI_REASONING_FALLBACK_MODEL=gemini-3.1-flash-lite

# Absolute or relative path to the source PDF
SOURCE_PDF=data/fy2024_analysis_of_revenue_and_expenditure.pdf
